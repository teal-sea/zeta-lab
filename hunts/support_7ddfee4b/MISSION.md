# MISSION: support run 7ddfee4b (descent arm, Erdős #126)

Support run for `0897a5a7` / `hunts/r_186989`. One bounded question:

> Assume an extremal or minimal counterexample $(S,A)$ and exploit scaling,
> gcd removal, parity, residue classes, and deletion of primes. Seek a
> recurrence for $g(k)$ whose per-prime loss tends to $1$ rather than $2$.
> Test the suggestion that omitting a small prime caps $|A|$.

**May write:** `hunts/support_7ddfee4b/`, and the case-log entry (Hunt #101) in
`hunts/README.md`.

**May not write:** any other `hunts/` directory, `zeta/`, `harness/`, `lean/`,
`meta/`, or any root markdown file.

Exploratory. Nothing here is a result, and nothing here is evidence for or
against RH (`docs/08`).
