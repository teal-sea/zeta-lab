mpmath>=1.3
numpy>=2.0
scipy>=1.14
matplotlib>=3.9
sympy>=1.13
pytest>=8.0
pytest-xdist>=3.6

# Arb ball arithmetic, the fast backend for zeta/rigor.py.  Optional by design
# — rigor.py falls back to mpmath's `iv` context and both backends are checked
# against each other — but the fallback is ~1600x slower on the certified
# paths, enough to turn a 0.8 s test into a 20-minute one and make the fast
# tier look hung.  Install it unless you are deliberately testing the fallback;
# `zeta.rigor.available_backends()` reports which are live.
python-flint>=0.6

# Convex/SDP solving, used by the frontier_math hunt's relaxation probes
# (`sos_certificate.py`, `cluster_sdp.py`, `configuration_lp.py`, ...).
#
# Undeclared until 2026-08-14, which cost a fresh clone **40 hard failures**
# rather than 40 skips: those tests call `relax(...)` directly and get a bare
# `ModuleNotFoundError`, so the suite reads as broken mathematics instead of a
# missing wheel.  The handoff of the day listed cvxpy as "already working"
# because the container it was written on had it installed ad hoc, which is
# exactly the kind of environment fact a snapshot cannot carry.
cvxpy>=1.5
