"""Runners for the experiments `meta/` has designed but could not execute.

`meta/asymmetry-experiment.md` is a pre-registered factorial whose *Factor B =
probabilistic* cells need a model in the loop at check time, plus per-artifact
cost accounting and a log a stranger can audit.  This package supplies that
runner, on top of Inspect (`inspect_ai`, UK AI Security Institute).

Nothing here is a mathematical result and nothing here trades against one, per
`meta/README.md`.  Nothing here has been *run* either: see
:mod:`meta.evals.asymmetry`.
"""
